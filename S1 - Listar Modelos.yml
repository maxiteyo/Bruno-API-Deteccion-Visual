info:
  name: S1 - Listar Modelos
  type: http
  seq: 1

http:
  method: GET
  url: "{{remoto}}/api/models"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
