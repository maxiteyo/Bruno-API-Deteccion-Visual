info:
  name: S3 - Obtener Fotograma
  type: http
  seq: 4

http:
  method: GET
  url: "{{local}}/api/frames/{frame_id}"  
  query:
    - name: thumbnail
      value: "true"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
